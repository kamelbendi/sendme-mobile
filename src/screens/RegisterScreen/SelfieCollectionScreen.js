import React from 'react';
import { View } from 'react-native';
import PropTypes from 'prop-types';
import Text from '../../components/Text';
//import * as styles from './LoginExistingScreen.styles';
//import { LoginExistingScreenWrapper } from './LoginExistingScreen.styles';

const SelfieCollectionScreen = (props) => (
  <View >
    <Text>Test content</Text>
  </View>
);

SelfieCollectionScreen.propTypes = {
  // bla: PropTypes.string,
};

SelfieCollectionScreen.defaultProps = {
  // bla: 'test',
};

export default SelfieCollectionScreen;
