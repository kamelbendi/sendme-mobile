import React from 'react';
import { View } from 'react-native';
import PropTypes from 'prop-types';
import Text from '../../components/Text';
//import * as styles from './LoginExistingScreen.styles';
//import { LoginExistingScreenWrapper } from './LoginExistingScreen.styles';

const AddressVerificationScreen = (props) => (
  <View >
    <Text>Test content</Text>
  </View>
);

AddressVerificationScreen.propTypes = {
  // bla: PropTypes.string,
};

AddressVerificationScreen.defaultProps = {
  // bla: 'test',
};

export default AddressVerificationScreen;
